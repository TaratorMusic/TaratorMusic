function renderStatistics() {
    // if (database = empty) return;
    // if (song listen = 0) return;

    // DRAW THESE ON PAPER!
        
    // Create two new tables under musics.db , music stats and flat stats.

    // Favorite song: will be selected from top time played.
    // Show all its stats: Listen time, listen amount, listen percentage,

    // Under these we have a TODO section, top language and top genre
    // Then, we will have 7 days, showing what percentage and amount of music listened
}